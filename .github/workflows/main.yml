name: Deploy Kútfő Plusz ERP

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Prepare authentication bridge
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          for name in ('auth-stable-fix.js','auth-password.js','login-fix.js','auth-bind.js'):
              pattern=r'<script[^>]+src=["\'](?:\./)?'+re.escape(name)+r'(?:\?[^"\']*)?["\'][^>]*></script>\s*'
              s=re.sub(pattern,'',s,flags=re.I)
          tag='<script src="auth-bind.js?v=20260818-4"></script>\n'
          if '</body>' not in s: raise SystemExit('index.html: missing </body>')
          s=s.replace('</body>',tag+'</body>')
          p.write_text(s,encoding='utf-8')
          PY

      - name: Validate authentication source
        run: |
          test -s supabase_config.js
          grep -q "__kutfoplusAuth" supabase_config.js
          test -s auth-bind.js
          grep -q "auth-bind.js?v=20260818-4" index.html
          test ! -e auth-stable-fix.js
          test ! -e auth-password.js
          test ! -e login-fix.js

      - name: Upload website
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
